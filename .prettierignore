dist
node_modules
test-results
foces-webv23
pnpm-lock.yaml
yarn.lock
LICENSE
# Tooling YAML — Prettier's YAML printer would reformat them for no benefit
.github
.coderabbit.yaml

# dotenv template — no prettier parser exists for it
.env.example

# Session/scratch notes (transcripts, not project content)
session-*.md
